# Campaign rules

Mỗi campaign một file `.json` cùng schema với `brand_kit.json`. Rule ở đây override rule global.

Ví dụ `clawathon-2026.json`:

```json
{
  "brand_terms": ["Campaign Name 2026"],
  "preferred_spellings": {
    "CampaignName 2026": "Campaign Name 2026"
  }
}
```
